# OAuth2 Configuration Example
# Copy this file to .env and fill in your values

# Vault Configuration (for OAuth2 credentials and encryption key)
VAULT_ENABLED=true
VAULT_URI=http://localhost:8200
VAULT_TOKEN=your-vault-root-token

# OAuth2 Frontend Callback URL
OAUTH2_FRONTEND_CALLBACK_URL=http://localhost:4200/auth/callback

# The following credentials should be stored in Vault at secret/atlasia
# and will be automatically loaded by the application:
#
# - oauth2.github.client-id
# - oauth2.github.client-secret
# - oauth2.google.client-id
# - oauth2.google.client-secret
# - oauth2.gitlab.client-id
# - oauth2.gitlab.client-secret
# - encryption.key (32-byte base64-encoded AES key)

# To set these in Vault, run:
# vault kv put secret/atlasia oauth2.github.client-id="your-github-client-id"
# vault kv put secret/atlasia oauth2.github.client-secret="your-github-client-secret"
# vault kv put secret/atlasia oauth2.google.client-id="your-google-client-id"
# vault kv put secret/atlasia oauth2.google.client-secret="your-google-client-secret"
# vault kv put secret/atlasia oauth2.gitlab.client-id="your-gitlab-client-id"
# vault kv put secret/atlasia oauth2.gitlab.client-secret="your-gitlab-client-secret"
# vault kv put secret/atlasia encryption.key="$(openssl rand -base64 32)"
