# Flyway Configuration
# Disable validation to bypass checksum mismatches (useful when scripts are modified for H2 compatibility)
spring.flyway.validate-on-migrate=false

# Escalation Insights Configuration
# Directory where escalation_insights.json reports will be saved
# Default: ./insights (relative to working directory)
# escalation.insights.output.dir=./insights
