# Database
DB_NAME=ai
DB_USER=ai
DB_PASSWORD=ai

# API Keys (use dummy values for local dev)
ORCHESTRATOR_TOKEN=dev-token-12345
LLM_API_KEY=sk-dev-key-placeholder

# JWT Authentication (generate a secure 256-bit secret for production)
JWT_SECRET_KEY=your-256-bit-secret-key-here-min-32-chars
JWT_ACCESS_TOKEN_EXPIRATION_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRATION_DAYS=7
JWT_ISSUER=atlasia-ai-orchestrator

# Optional: Set to false to disable analytics in dev
ANALYTICS_ENABLED=false
