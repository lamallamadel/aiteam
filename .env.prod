# ============================================================================
# AITEAM PRODUCTION ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to /prod/.env and fill in values before deployment
# NEVER commit this file with real secrets — use GitHub Secrets for CI/CD

# ============================================================================
# DATABASE
# ============================================================================
DB_NAME=aiteam
DB_USER=aiteam
# REQUIRED: Set strong password (min 20 chars, mix of upper/lower/numbers/symbols)
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION_DO_NOT_USE_DEFAULT

# ============================================================================
# APPLICATION
# ============================================================================
# REQUIRED: GitHub repository (e.g., afroware/aiteam)
GITHUB_REPO=afroware/aiteam

# REQUIRED: Domain name for SSL certificates (e.g., api.example.com)
DOMAIN=api.example.com

# REQUIRED: JWT/Auth token for orchestrator
ORCHESTRATOR_TOKEN=CHANGE_ME_IN_PRODUCTION_MIN_32_CHARS_RANDOM

# REQUIRED: LLM API key (for language model integration)
LLM_API_KEY=CHANGE_ME_IN_PRODUCTION_YOUR_LLM_API_KEY

# ============================================================================
# NOTIFICATIONS (Optional)
# ============================================================================
# Slack webhook for deployment notifications (GitHub Actions)
# Get from: https://api.slack.com/apps
SLACK_WEBHOOK=

# ============================================================================
# SECURITY
# ============================================================================
# Allowed CORS origins (separate multiple with comma)
CORS_ALLOWED_ORIGINS=https://api.example.com,https://example.com

# ============================================================================
# NOTES
# ============================================================================
# 1. All REQUIRED fields must be filled before deployment
# 2. Generate strong random strings:
#    Linux/Mac: openssl rand -base64 32
#    Windows: [Convert]::ToBase64String((1..32 | ForEach-Object {[byte]$_}))
# 3. Store real values in GitHub Secrets (Settings > Secrets)
# 4. For production backups:
#    - Database: PostgreSQL backup daily via pg_dump
#    - Config: Store .env in secure vault
# 5. Rotate secrets quarterly
