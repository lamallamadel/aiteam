# Atlasia — AI Governance Pack

Ce dossier contient:
- Contrats d'agents (`ai/agents`)
- Schémas JSON d'artefacts (`ai/schemas`)
- Playbooks opératoires (`ai/playbooks`)
- Personas ("Gems") via `ai/agents/personas`

L'objectif: exécution autonome via GitHub Issues + GitHub Actions, et dialogue interactif via le moteur **Dual-Mode** (Code vs Chat).
Gouverné par `docs/QUALITY_GATES.md`.
