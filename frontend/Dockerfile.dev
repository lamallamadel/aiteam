FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# Expose port 4200
EXPOSE 4200
# Run ng serve with polling for hot-reload in Docker/Windows
CMD ["npm", "run", "start", "--", "--host", "0.0.0.0", "--poll", "2000"]
